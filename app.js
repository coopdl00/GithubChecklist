knex(humps(bcrypt(express(axios(get(fetch(next(Promise(Math.floor(round(then((wat) => {
                      }) => {
                    }) => {
                  }) => {
                }) => {
              }) => {
            }) => {
          }) => {
        }) => {
      }) => {
    }) => {
  }) => {
}) => {
}
hi im paul
wat